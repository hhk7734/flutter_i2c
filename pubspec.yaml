name: flutter_i2c
description: I2C
version: 0.0.1
repository: https://github.com/hhk7734/flutter_i2c

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0
  ffigen: ^2.4.2

flutter:
  plugin:
    platforms:
      linux:
        pluginClass: FlutterI2cPlugin
