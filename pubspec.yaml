name: flutter_i2c
description: I2C
version: 0.0.1
repository: https://github.com/hhk7734/flutter_i2c

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.2

flutter:
  plugin:
    platforms:
      linux:
        pluginClass: FlutterI2cPlugin
